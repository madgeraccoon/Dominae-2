**Dominae** — a Discord screengrab bot by Elisha Shaddock
`Dominae Commands`
`PREFIXhelp` shows this message. Who'd have thought?
`PREFIXweb` Takes a picture through my webcam
`PREFIXmov` Takes a short clip through my webcam
`PREFIXfull` Takes a full screenshot of my monitors
`PREFIXwindow` Takes a screenshot of the current window I'm using
`PREFIXselect` Forces me to select an area to screenshot
`PREFIXserver` Legacy command from Dominae; disabled
`PREFIXservecam` Legacy command from Dominae; disabled
`PREFIXsay` Generates a text image out of some text
`PREFIXvox` Generates an audio VOX sound. Use `PREFIXhelp PREFIXvox` for more info
`$prefix` Sets the bot prefix. Default is s
`$on / $off` Enables or Disables all bot functions